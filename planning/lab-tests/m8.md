# Lab-test evidence — M8: Securing & Governing AI Workloads

**Machine:** Apple Silicon (arm64) · Rancher Desktop · **syft, trivy 0.72, grype, cosign 3.1** (brew)
**Date:** 2026-07-05
**Target:** `acme-support-agent:latest` (the M6 agent image, Debian 13 + Python 3.12).

## SBOM — Syft

```
$ syft acme-support-agent:latest -o spdx-json > sbom.spdx.json
   -> 96 packages, SPDX-2.3   (Debian debs + Python + binaries)
```

## Vulnerability scan — Trivy and Grype

```
$ trivy image --scanners vuln --severity CRITICAL,HIGH,MEDIUM acme-support-agent:latest
   Total: 64 (MEDIUM: 53, HIGH: 9, CRITICAL: 2)     e.g. gzip CVE-2026-41992 (HIGH), libacl1 CVE-2026-54369 (HIGH)
$ grype acme-support-agent:latest
   by severity: Critical 5, High 28, Medium 60, Low 7, Negligible 51
```

Two scanners, overlapping-but-different findings — the lesson: scanners disagree, so run more than one and
triage by fixable + severity. (Remediation = rebuild on a patched base / bump packages, then re-scan.)

## Sign & verify — Cosign (key-based, offline)

```
$ cosign generate-key-pair                      # cosign.key + cosign.pub (empty passphrase for the lab)
$ cosign sign   --key cosign.key <registry>/acme-support-agent:1.0.0
$ cosign verify --key cosign.pub <registry>/acme-support-agent:1.0.0
   -> "The signatures were verified against the specified public key"
```

(Validated against a local `registry:2`; GHCR works identically once you have a `write:packages` token.)

## Sandboxing generated / tool code — locked-down ephemeral container

```
$ docker run --rm --network none --read-only --cap-drop ALL --pids-limit 64 --memory 256m \
    python:3.12-slim python -c "print('sandboxed result:', sum(range(10)))"
   -> sandboxed result: 45
# and isolation is real — network egress is blocked:
$ docker run --rm --network none --cap-drop ALL python:3.12-slim \
    python -c "import urllib.request; urllib.request.urlopen('http://example.com', timeout=3)"
   -> URLError (network unreachable)
```

This is how the crew's Fixer would run any generated/tool code: no network, read-only rootfs, all
capabilities dropped, resource-capped, thrown away after one use (gVisor/ToolHive add deeper isolation).

## Verdict

✅ The open-source supply chain works end-to-end on a laptop: **Syft** SBOM → **Trivy/Grype** scan →
**Cosign** sign+verify, plus **sandboxed** code execution with proven network isolation. Wire these into
a GitHub Actions `build → scan → sign` pipeline (see `labs/m8/security-pipeline.yml`).

## checks.json validation 2026-07-22

Authored `labs/m8/checks.json` + `up.sh`/`down.sh` (deterministic, no long-running services — `up.sh`
just ensures the scan target `acme-support-agent:latest` exists locally, building it from `labs/m6/` if
missing) + `labs/m8/guardrail-eval.py` (extracted from lab.md Step 7's inline heredoc, verbatim logic).
All `syft`/`trivy`/`grype` calls pin `DOCKER_HOST=unix://$HOME/.rd/docker.sock` inline so they scan the
local Rancher Desktop image store without a registry pull.

```
$ ./labs/m8/up.sh
m8 ready: acme-support-agent:latest present locally.

$ node scripts/run-checks.mjs labs/m8/checks.json
✅ syft-sbom-packages — syft SBOM (spdx-json) of the local lab image lists packages
✅ trivy-scan-gated-exit0 — trivy vuln scan of the local image runs with an explicit exit-code gate and exits 0
✅ grype-second-opinion — grype produces a second vulnerability-scan opinion on the local image
✅ cosign-present — cosign CLI is installed and runs
✅ sandbox-network-isolated — the sandbox proves egress is genuinely blocked for generated code (URLError: unreachable or DNS resolution fails under --network none)
✅ guardrail-eval-passes — the M6 agent's guardrail mini-eval passes deterministically (no model, no ChromaDB)

6/6 checks · score 6/6
```

**Two drift findings, both fixed:**

1. **SBOM format key.** lab.md authors `syft ... -o spdx-json`, which nests packages under
   `"packages"` (SPDX schema), not `"artifacts"` (Syft's native `-o json` format). Asserted on
   `"packages"` — matches the format the lab actually produces (still 96 packages, matching the
   earlier validation).
2. **Sandbox network-isolation error text.** On this machine, `docker run --network none` fails DNS
   resolution first (`URLError: <urlopen error [Errno -3] Temporary failure in name resolution>`), not
   `Network is unreachable` as originally documented — same effective isolation (a `URLError`, egress
   never reached), different errno depending on the runtime. Updated the "Expected output" block in
   `site/docs/m8-security/lab.md` to show the real captured error and note the variance; the check
   asserts the runtime-independent `"URLError"` substring.

`grype`'s installed version (0.115.0) does not print the older `"Vulnerabilities by severity: ..."`
summary line seen in the original validation — its table output (header `VULNERABILITY`/`SEVERITY`) is
still present and asserted on; `lab.md`'s illustrative "Expected output" already carries a
"versions will vary" caveat for these four tools, so no lab.md edit was needed for that block.

`./labs/m8/down.sh` verified idempotent (run twice back to back, both exit 0).

**Verdict:** ✅ m8 checks green (6/6).
