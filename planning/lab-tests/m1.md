# Lab-test evidence — M1: Prove the Wiring

**Machine:** Apple Silicon (arm64), 16 GB RAM  
**Date:** 2026-07-05  
**Runtime:** Rancher Desktop — docker client `29.5.3-rd`, server `29.5.2` (dockerd/moby)  
**Model server:** Ollama `0.17.4` (native, Metal), serving on `:11434`  
**Model:** `qwen2.5:1.5b` (986 MB, Q4_K_M)  
**Validated by:** automated Bash tool calls with `PATH="$HOME/.rd/bin:$PATH"` prefix

> Note: in lab.md, all commands use plain `docker` (learners have it on PATH).
> In this validation session, every docker command was prefixed with `PATH="$HOME/.rd/bin:$PATH"`.

---

## Lab Step 1 — Start your runtime / `docker version`

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker version
```

**Real output:**
```
Client:
 Version:           29.5.3-rd
 API version:       1.54
 Go version:        go1.26.4
 Git commit:        5d9ffe3
 Built:             Thu Jun  4 07:33:36 2026
 OS/Arch:           darwin/arm64
 Context:           rancher-desktop

Server:
 Engine:
  Version:          29.5.2
  API version:      1.54 (minimum version 1.41)
  Go version:       go1.25.10
  Git commit:       568f755ebeb1ac9c6a8febbda6cd371ea0a9630b
  Built:            Sun May 31 13:53:18 2026
  OS/Arch:          linux/arm64
  Experimental:     false
 containerd:
  Version:          v2.3.2
  GitCommit:        fff62f14765df376e5fc36f5a8f8e795b5670f61
 runc:
  Version:          1.4.0
  GitCommit:        8bd78a9977e604c4d5f67a7415d7b8b8c109cdc4
 docker-init:
  Version:          0.19.0
```

**Status:** ✅ PASS — both Client and Server sections present.

---

## Lab Step 2a — `ollama list`

**Command:**
```bash
ollama list
```

**Real output:**
```
NAME            ID              SIZE      MODIFIED
qwen2.5:1.5b    65ec06548149    986 MB    21 minutes ago
```

**Status:** ✅ PASS — `qwen2.5:1.5b` present.

---

## Lab Step 2b — `curl http://localhost:11434/api/tags`

**Command:**
```bash
curl -s http://localhost:11434/api/tags
```

**Real output:**
```json
{"models":[{"name":"qwen2.5:1.5b","model":"qwen2.5:1.5b","modified_at":"2026-07-05T13:12:09.965883529+05:30","size":986061892,"digest":"65ec06548149b04c096a120e4a6da9d4017ea809c91734ea5631e89f96ddc57b","details":{"parent_model":"","format":"gguf","family":"qwen2","families":["qwen2"],"parameter_size":"1.5B","quantization_level":"Q4_K_M"}}]}
```

**Status:** ✅ PASS — native API responds with model listing.

---

## Lab Step 3 — Core move: call model from inside a container

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker run --rm curlimages/curl:latest -s \
  http://host.docker.internal:11434/api/generate \
  -d '{"model":"qwen2.5:1.5b","prompt":"Say hi in 5 words.","stream":false}'
```

**Real output:**
```json
{"model":"qwen2.5:1.5b","created_at":"2026-07-05T08:04:05.023775Z","response":"Hello there! How can I help you today?","done":true,"done_reason":"stop","context":[...],"total_duration":1389733333,"load_duration":1043020167,"prompt_eval_count":36,"prompt_eval_duration":202138750,"eval_count":11,"eval_duration":128461332}
```

**Status:** ✅ PASS — container reached native Ollama via `host.docker.internal:11434` and received a valid `"response"` field.

**Deviation from lab.md spec:** Response text was "Hello there! How can I help you today?" (generative — varies per run). lab.md expected output updated to match this capture. The `...` pattern in expected output already acknowledged variance; updated placeholder text to match captured value for accuracy.

---

## Lab Step 4a — Run `call-ollama.sh` (default prompt)

**Setup:**
```bash
chmod +x labs/m1/call-ollama.sh
```

**Command (with PATH for validation):**
```bash
PATH="$HOME/.rd/bin:$PATH" ./labs/m1/call-ollama.sh "Explain what a container is in one sentence."
```

**Real output:**
```json
{"model":"qwen2.5:1.5b","created_at":"2026-07-05T08:04:13.2483Z","response":"A container is a lightweight virtualization technology that allows you to package an application and all its dependencies into a single unit, which can then be easily deployed across different computing environments.","done":true,"done_reason":"stop","context":[...],"total_duration":571176125,"load_duration":84225584,"prompt_eval_count":39,"prompt_eval_duration":68593333,"eval_count":36,"eval_duration":401166037}
```

**Status:** ✅ PASS — script executed, container called native Ollama, received coherent response.

**Deviation from lab.md spec:** Response text updated from a shorter placeholder to match captured output.

---

## Lab Step 4b — Run `call-ollama.sh` (custom prompt)

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" ./labs/m1/call-ollama.sh "What is a container registry in one sentence?"
```

**Real output:**
```json
{"model":"qwen2.5:1.5b","created_at":"2026-07-05T08:04:18.651359Z","response":"A container registry is an online repository for storing and managing Docker images.","done":true,"done_reason":"stop","context":[...],"total_duration":333192584,"load_duration":84275084,"prompt_eval_count":38,"prompt_eval_duration":73400916,"eval_count":15,"eval_duration":163636459}
```

**Status:** ✅ PASS — custom prompt works.

**Deviation from lab.md spec:** Response text updated to match real capture ("online repository for storing and managing Docker images" vs placeholder text).

---

## Summary

| Step | Command | Result | Notes |
|------|---------|--------|-------|
| 1 | `docker version` | ✅ PASS | Client 29.5.3-rd, Server 29.5.2 |
| 2a | `ollama list` | ✅ PASS | qwen2.5:1.5b 986 MB present |
| 2b | `curl .../api/tags` | ✅ PASS | Native API responds |
| 3 | `docker run curlimages/curl` | ✅ PASS | Container → host.docker.internal:11434 works |
| 4a | `./call-ollama.sh` default | ✅ PASS | Script works, response received |
| 4b | `./call-ollama.sh` custom | ✅ PASS | Custom prompt works |

**All lab commands work as written. Three expected-output strings were updated in lab.md to match captured reality (generative response text varies per run — format/structure unchanged).**

No structural command changes were required. The `host.docker.internal` hostname resolves correctly from inside a Rancher Desktop container.
