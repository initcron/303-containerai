# Lab-test evidence — M5: Docs Assistant (Naive RAG)

**Machine:** Apple Silicon (arm64), 16 GB · Rancher Desktop · **native Ollama**
**Date:** 2026-07-05
**Stack:** ChromaDB `0.5.20` (container) + Streamlit app (container) + native Ollama
**Models:** `qwen2.5:1.5b` (generation) + `nomic-embed-text` (embeddings, 768-dim) — both native.

> Apple-Silicon pattern: model server NATIVE; ChromaDB + app containerized, reaching Ollama at
> `host.docker.internal:11434`. This is the reuse `lightweight-genai-stack` adapted off its
> *containerized* Ollama onto native Ollama, and pinned Chroma to 0.5.x to match the client.

## ChromaDB container — up

```
$ docker run -d -p 8000:8000 chromadb/chroma:0.5.20
$ curl -s localhost:8000/api/v2/heartbeat   ->  {"nanosecond heartbeat": 1783255100...}
```

## Native embeddings — work

```
$ curl -s localhost:11434/api/embeddings -d '{"model":"nomic-embed-text","prompt":"..."}'
   ->  embedding with 768 dimensions
```

## Full naive-RAG round-trip — validated headlessly

Ingested 3 Acme runbook chunks (embedded via `nomic-embed-text`) into a Chroma collection, then
queried and generated a grounded answer with `qwen2.5:1.5b`:

```
[ingest]   stored 3 chunks in Chroma collection 1c610dd7
[retrieve] top chunk (dist=164.777):
           "To restart the Acme payments service, run: kubectl rollout restart deploy/payments -n prod."
[generate] "To restart the payments service, you need to execute the following command:
            kubectl rollout restart deploy/payments -n prod"
```

The query *"How do I restart the payments service?"* retrieved the correct runbook chunk by semantic
similarity and the model answered **grounded in that retrieved context** — the whole naive-RAG loop
(ingest → embed → store → retrieve → generate) working on native Ollama + a containerized vector DB.

## App image — trimmed for the laptop budget

`main.py` imports only the `langchain-ollama` / `langchain-chroma` stack — **not**
`sentence-transformers`/`torch`. Dropped `sentence-transformers` (≈2 GB torch) and unused
`python-docx` from `requirements.txt`, keeping the RAG stack + `pypdf` + `unstructured`/`markdown`
loaders. (App build + Streamlit health validation appended below.)

App build + full-stack health (trimmed image):

```
$ docker compose up -d          # chromadb + genai-app (app builds from ./app)
$ curl -s -o /dev/null -w '%{http_code}' localhost:8000/api/v2/heartbeat   -> 200   (ChromaDB)
$ curl -s -o /dev/null -w '%{http_code}' localhost:8501/_stcore/health     -> 200   (Streamlit)
$ docker logs genai-app | grep Streamlit
   "You can now view your Streamlit app in your browser.  URL: http://0.0.0.0:8501"
```

✅ The Docs Assistant image builds and runs on Rancher Desktop, serving the Streamlit UI on :8501,
wired to native Ollama (`host.docker.internal:11434`) + ChromaDB. `docker compose down` frees RAM.

## Verdict (pipeline)

✅ Naive RAG works end-to-end on native Ollama + ChromaDB(0.5.20) on Rancher Desktop, with a real
grounded answer over an Acme runbook corpus.

## checks.json validation 2026-07-22

Authored `labs/m5/checks.json` + `up.sh`/`down.sh` (m2/m3 pattern: PATH prefix, poll-not-sleep,
idempotent teardown) and `labs/m5/rag_roundtrip.py` — a scripted headless RAG round-trip (the
Streamlit app has no headless API, so the check drives ChromaDB HTTP + native Ollama directly, exactly
as validated above) that asserts the answer contains the deterministic runbook command.

**Lab drift found + fixed:** `lab.md` documented a `./docs:/app/seed-docs:ro` volume mount on
`genai-app` that the actual `compose.yaml`/`main.py` never had or read (docs are ingested via the
Streamlit file-uploader, not a mounted seed path). Removed the stale mount block + its bullet from
`site/docs/m5-naive-rag/lab.md`; no compose/app behavior changed.

```
$ ./labs/m5/up.sh
 ... Image m5-genai-app Built
 ... Container chromadb Started
 ... Container genai-app Started
m5 ready: chromadb + genai-app healthy.

$ node scripts/run-checks.mjs labs/m5/checks.json
✅ chromadb-heartbeat — ChromaDB container answers the v2 heartbeat
✅ streamlit-health — genai-app Streamlit health endpoint returns 200
✅ app-log-streamlit-started — genai-app log shows Streamlit came up
✅ rag-roundtrip-grounded-answer — scripted ingest->embed->retrieve->generate round-trip returns the exact runbook command

4/4 checks · score 4/4
```

`rag_roundtrip.py` output (evidence of the grounded answer the check asserts against):

```
[retrieved] ## Payments service
To restart the Acme payments service, run:
`kubectl rollout restart deploy/payments -n prod`.
The payments service depends on the Postgres primary in the `prod` namespace.
[answer] Run the following command:

```bash
kubectl rollout restart deploy/payments -n prod
```

This command will restart the Acme payments service, which is deployed as a deployment in the `prod` namespace.
```

`./labs/m5/down.sh` verified idempotent (removed containers + `m5_default` network cleanly; re-run
after teardown exits 0 with nothing to remove).

**Verdict:** ✅ m5 checks green (4/4).
