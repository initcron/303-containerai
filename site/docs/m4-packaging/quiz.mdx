---
sidebar_position: 3
title: 'Quiz: Module 4'
---

import Quiz from '@site/src/components/Quiz';

# Module 4 Quiz

Test your understanding of OCI artifacts for models, ModelKit layers, selective pull, GHCR authentication, and KitOps versus `docker model package` from the lesson and lab.

<Quiz questions={[
  {
    prompt: 'Why are OCI artifacts a better fit for distributing ML models than plain container images? (Select all that apply)',
    multiSelect: true,
    options: [
      {
        text: 'OCI artifacts support typed layers (model, code, datasets, docs) that enable selective pull — you can fetch only the model weights without downloading code or datasets.',
        correct: true,
        explanation: 'Correct. ModelKit layer types are first-class in the format. A plain container image has no equivalent concept — every COPY layer is opaque bytes with no semantic meaning registries or CLIs can act on.',
      },
      {
        text: 'OCI artifacts can be stored in any OCI-compliant registry (GHCR, Docker Hub, Quay, Harbor, local registry:2) using standard tooling — no special model repository required.',
        correct: true,
        explanation: 'Correct. KitOps uses ORAS (OCI Registry as Storage) under the hood, which speaks the standard OCI distribution API. The same infrastructure that handles your container images handles your ModelKits.',
      },
      {
        text: 'OCI artifacts compress model weights to near-zero size, making them faster to transfer than raw files.',
        correct: false,
        explanation: 'OCI does not add special compression beyond what each blob already has. The GGUF file is stored as-is. The benefit is versioning, typing, and distribution — not magical compression.',
      },
      {
        text: 'Registry-level deduplication means if only the prompt config changes between versions, only the changed layer is pushed — the weights layer is already in the registry.',
        correct: true,
        explanation: 'Correct. Because each layer is content-addressed by its SHA-256 digest, the registry skips uploading blobs it already has. If your 100 MB model is unchanged between v1.0.0 and v1.0.1, only the config layer is pushed.',
      },
    ],
  },
  {
    prompt: 'In a ModelKit, the Kitfile plays the role of the "shipping manifest." What does this mean technically?',
    options: [
      {
        text: 'The Kitfile is a Dockerfile variant that kit uses to build a runnable container image from the model weights.',
        correct: false,
        explanation: 'The Kitfile produces an OCI artifact, not a runnable container image. It has no entrypoint, no base OS layer, and is never "run" — the weights are unpacked and loaded by a separate runtime.',
      },
      {
        text: 'The Kitfile is a YAML file that declares the package metadata, model path, code files, and dataset references. When kit pack runs, it reads the Kitfile to produce typed OCI layers and a signed manifest.',
        correct: true,
        explanation: 'Correct. manifestVersion, package, model, code, and datasets in the Kitfile map directly to OCI layer types and manifest annotations. kit pack reads it, hashes each file into a content-addressed blob, and writes the OCI manifest that ties everything together.',
      },
      {
        text: 'The Kitfile is a lockfile that pins the exact Python dependency versions used during model training.',
        correct: false,
        explanation: 'The Kitfile describes the model artifact structure — weights, code, and datasets — not Python dependencies. It is closer to a Dockerfile or helm Chart.yaml than a requirements.txt.',
      },
      {
        text: 'The Kitfile is generated automatically by kit after the model is pushed; you cannot author it by hand.',
        correct: false,
        explanation: 'The Kitfile is hand-authored (or generated by tooling) before packing. It lives at the root of your model workspace and is the input to kit pack, not an output.',
      },
    ],
  },
  {
    prompt: 'A serving node only needs the model weights, not the training datasets or inference scripts. Which kit command fetches only the model layer?',
    options: [
      {
        text: 'kit pull --layer=weights ghcr.io/org/model:1.0.0',
        correct: false,
        explanation: 'There is no --layer flag in kit. The correct flag is --filter, and the valid type keyword is "model" not "weights".',
      },
      {
        text: 'kit unpack ghcr.io/org/model:1.0.0 --filter=model -d ./weights-only',
        correct: true,
        explanation: 'Correct. --filter accepts the layer type names defined in the Kitfile: model, code, datasets, docs, prompts. Passing --filter=model fetches only the model layer blob, skipping everything else.',
      },
      {
        text: 'kit pull ghcr.io/org/model:1.0.0 --no-code --no-datasets',
        correct: false,
        explanation: 'kit does not have a pull sub-command with exclude flags. The correct workflow is kit unpack with --filter to include specific layer types.',
      },
      {
        text: 'kit unpack ghcr.io/org/model:1.0.0 --skip-code --skip-datasets -d ./weights-only',
        correct: false,
        explanation: 'kit does not have --skip-* flags. The pattern is inclusive: --filter=model says "give me the model layer" rather than "exclude everything else."',
      },
    ],
  },
  {
    prompt: 'You run kit push ghcr.io/yourorg/acme-docs-model:1.0.0 and receive: "denied: permission_denied: token ... scopes". What is the root cause and fix?',
    options: [
      {
        text: 'The ModelKit is too large for GHCR. You must use Docker Hub for files over 50 MB.',
        correct: false,
        explanation: 'GHCR has no 50 MB limit for OCI artifacts. The "scopes" error is a token permissions problem, not a size problem.',
      },
      {
        text: 'Your gh token lacks the write:packages scope. Fix: gh auth refresh -h github.com -s write:packages, then pipe the new token into kit login ghcr.io --password-stdin.',
        correct: true,
        explanation: 'Correct. The default gh token only has repo and read:packages. Pushing OCI artifacts to GHCR requires write:packages. After refreshing, you also need to make the package public in GHCR settings if you want unauthenticated pulls.',
      },
      {
        text: 'kit does not support GHCR. Switch to kit push docker.io/yourorg/acme-docs-model:1.0.0.',
        correct: false,
        explanation: 'kit supports any OCI-compliant registry including GHCR. The error is a token scope issue, not a registry compatibility issue.',
      },
      {
        text: 'You must add --plain-http to the push command because GHCR does not support TLS.',
        correct: false,
        explanation: '--plain-http is for HTTP-only registries like a local registry:2. GHCR uses HTTPS/TLS and does not need (or accept) this flag. Adding it to a GHCR push would cause a different error.',
      },
    ],
  },
  {
    prompt: 'How does KitOps ModelKit differ from docker model package, and why does that matter for production deployments?',
    options: [
      {
        text: 'They are functionally identical; docker model package is just the older name for the same project.',
        correct: false,
        explanation: 'They are distinct products. KitOps is a CNCF project with its own CLI (kit) and multi-layer format. docker model package is a Docker Desktop 4.40+ feature with a different scope and registry target.',
      },
      {
        text: 'docker model package supports selective pull; KitOps does not.',
        correct: false,
        explanation: 'It is the opposite. KitOps has first-class selective pull via --filter. docker model package packs the model as a single blob with no per-layer type filtering.',
      },
      {
        text: 'KitOps is a CNCF project that works on any OCI registry with typed layers and selective pull; docker model package is Docker Desktop-specific, targets Docker Hub primarily, and has no --filter equivalent — making KitOps the portable choice for multi-registry and air-gapped deployments.',
        correct: true,
        explanation: 'Correct. KitOps integrates with the CNCF ecosystem (Flux, Harbor, Argo CD) and runs on any OCI registry via ORAS. docker model package works within the Docker Desktop ecosystem and is less portable across registries and orchestration tools.',
      },
      {
        text: 'KitOps requires Kubernetes; docker model package works without a cluster.',
        correct: false,
        explanation: 'KitOps does not require Kubernetes. kit is a standalone CLI that works against any OCI registry, locally or remotely, with no cluster needed.',
      },
    ],
  },
]} />
