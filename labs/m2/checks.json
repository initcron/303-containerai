{
  "checks": [
    { "id": "ollama-v1-models", "describe": "native Ollama OpenAI-compatible /v1/models lists qwen2.5:1.5b",
      "run": "curl -s http://localhost:11434/v1/models",
      "assert": { "contains": "qwen2.5:1.5b" } },
    { "id": "compose-build", "describe": "m2 compose client image builds",
      "run": "PATH=\"$HOME/.rd/bin:$PATH\" docker compose -f labs/m2/compose.yaml build",
      "assert": { "exit": 0 } },
    { "id": "client-answers-via-v1", "describe": "containerized client reaches Ollama /v1 through host.docker.internal and gets a chat completion",
      "run": "PATH=\"$HOME/.rd/bin:$PATH\" docker compose -f labs/m2/compose.yaml run --rm client",
      "assert": { "exit": 0 } },
    { "id": "compose-teardown", "describe": "compose down removes the m2 network cleanly (teardown-safe)",
      "run": "PATH=\"$HOME/.rd/bin:$PATH\" docker compose -f labs/m2/compose.yaml down",
      "assert": { "exit": 0 } }
  ]
}
